<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing trip</h1>

  <%= render "form", trip: @trip %>

  <%= link_to "Show this trip", user_trip_path(@trip), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to trips", user_trips_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
